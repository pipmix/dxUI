#include "Text.h"
using namespace std;

int main() {

	Text text;
	text.LoadFont();


	//if (velocity.x > 0.0f)spr01.colDir | 1;
	//else if (velocity.x < 0.0f)spr01.colDir | 4;
	//if (velocity.y > 0.0f)spr01.colDir | 2;
	//else if (velocity.y < 0.0f)spr01.colDir | 8;


	return 0;
}